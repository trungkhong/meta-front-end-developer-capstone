import styles from './Loading.module.css';

const Loading = () => {
  return (
    <div className={styles.loading}>
      <h1 className={styles.loading_h1}>Loading...</h1>
    </div>
  );
};

export default Loading;
